export interface Product {
  id?: string;
  title?: string;
  description?: string;
  imagePath?: string;

  // constructor(id: string, name: string, desc: string, imagePath: string) {
  //   this.id = id;
  //   this.title = name;
  //   this.description = desc;
  //   this.imagePath = imagePath;
  // }
  //
  // addToShoppingCart(name: string, amount: number) {
  //   this.title = name;
  //   this.amount = amount;
  // }
}
